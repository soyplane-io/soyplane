---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: tofumodules.opentofu.soyplane.io
spec:
  group: opentofu.soyplane.io
  names:
    kind: TofuModule
    listKind: TofuModuleList
    plural: tofumodules
    shortNames:
    - tfmod
    singular: tofumodule
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.source
      name: Source
      type: string
    - jsonPath: .spec.version
      name: Version
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: TofuModule is the Schema for the tofumodules API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TofuModuleSpec defines the desired state of a TofuModule
              resource.
            properties:
              backend:
                description: BackendSpec defines the backend configuration for state
                  storage of a module.
                properties:
                  config:
                    additionalProperties:
                      x-kubernetes-preserve-unknown-fields: true
                    type: object
                  rawConfig:
                    type: string
                  type:
                    type: string
                  valueSources:
                    additionalProperties:
                      description: ValueFrom defines how to retrieve values from external
                        sources.
                      properties:
                        configMapRef:
                          description: KeyRef contains a reference to a key in a secret
                            or config map.
                          properties:
                            key:
                              type: string
                            name:
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        secretRef:
                          description: KeyRef contains a reference to a key in a secret
                            or config map.
                          properties:
                            key:
                              type: string
                            name:
                              type: string
                          required:
                          - key
                          - name
                          type: object
                      type: object
                    type: object
                required:
                - type
                type: object
              outputs:
                items:
                  description: OutputSpec defines the output configuration for a module.
                  properties:
                    from:
                      type: string
                    to:
                      items:
                        description: OutputTarget defines the target for the output
                          of a module.
                        properties:
                          key:
                            type: string
                          kind:
                            description: Kind indicates the type of output target
                              object.
                            enum:
                            - Secret
                            - ConfigMap
                            type: string
                          name:
                            type: string
                        required:
                        - key
                        - kind
                        - name
                        type: object
                      type: array
                  required:
                  - from
                  - to
                  type: object
                type: array
              providers:
                items:
                  description: TofuProviderRef contains a reference to a Tofu provider.
                  properties:
                    alias:
                      type: string
                    name:
                      type: string
                  required:
                  - name
                  type: object
                type: array
              source:
                type: string
              valueSources:
                additionalProperties:
                  description: ValueFrom defines how to retrieve values from external
                    sources.
                  properties:
                    configMapRef:
                      description: KeyRef contains a reference to a key in a secret
                        or config map.
                      properties:
                        key:
                          type: string
                        name:
                          type: string
                      required:
                      - key
                      - name
                      type: object
                    secretRef:
                      description: KeyRef contains a reference to a key in a secret
                        or config map.
                      properties:
                        key:
                          type: string
                        name:
                          type: string
                      required:
                      - key
                      - name
                      type: object
                  type: object
                type: object
              variables:
                additionalProperties:
                  x-kubernetes-preserve-unknown-fields: true
                type: object
              version:
                type: string
            required:
            - backend
            - source
            type: object
          status:
            description: TofuModuleStatus defines the observed state of a TofuModule.
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
