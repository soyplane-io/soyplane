---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: tofuproviders.opentofu.soyplane.io
spec:
  group: opentofu.soyplane.io
  names:
    kind: TofuProvider
    listKind: TofuProviderList
    plural: tofuproviders
    shortNames:
    - tfprov
    singular: tofuprovider
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.type
      name: Type
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: TofuProvider is the Schema for the tofuproviders API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TofuProviderSpec defines the configuration for a Terraform/OpenTofu
              provider block.
            properties:
              config:
                additionalProperties:
                  x-kubernetes-preserve-unknown-fields: true
                description: Config is a templated YAML configuration parsed into
                  structured JSON.
                type: object
              rawConfig:
                description: RawConfig is a raw HCL/YAML block (stringified).
                type: string
              type:
                description: Type is the name of the provider (e.g., "aws", "google").
                type: string
              valueSources:
                additionalProperties:
                  properties:
                    configMapRef:
                      properties:
                        key:
                          description: Key is the specific key within the ConfigMap.
                          type: string
                        name:
                          description: ConfigMapKeyRef identifies a key within a ConfigMap.
                          type: string
                      required:
                      - key
                      - name
                      type: object
                    secretRef:
                      description: ValueSource represents where to pull a single value
                        from (secret or config map).
                      properties:
                        key:
                          description: Key is the specific key within the Secret.
                          type: string
                        name:
                          description: SecretKeyRef identifies a key within a Kubernetes
                            Secret.
                          type: string
                      required:
                      - key
                      - name
                      type: object
                  type: object
                description: ValueSources maps variables to secrets or config maps.
                type: object
            required:
            - type
            type: object
          status:
            description: TofuProviderStatus holds observed state (currently unused).
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
